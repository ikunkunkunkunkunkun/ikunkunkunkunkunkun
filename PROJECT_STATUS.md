# Project Status

- Current goal: Keep the public README snake's green cells aligned with the same rolling-year contribution window used by the native GitHub graph.
- Completed work: Kept the top split quote and illustration and short introduction. Split the generated metrics into two cards in one row. Replaced the generic snake action with a token-backed GraphQL generator that reads the user's rolling 365-day contribution cells and renders the same green levels with a purple snake over them. The README still embeds the public SVG for all visitors.
- Key technical decisions: Use GitHub-supported HTML table layout with 50% columns and one distinct metrics SVG per column. The second metrics action uses `base: ""` to avoid repeating profile data. Both action steps now use a repository secret backed by the authenticated user's token for account-wide access; `GITHUB_TOKEN` remains only the committer token. Forks are included in repository counts.
- Core file changes: `README.md`, `scripts/generate-public-snake.mjs`, `.github/workflows/snake.yml`, `PROJECT_STATUS.md`; earlier work changed `assets/profile-art.png` and `.github/workflows/metrics.yml`.
- Verification: The old `gh-pages` snake SVG was only 48×96 and contained four snake cells, with no contribution grid. V3 workflow run `35430575725` succeeded; the new SVG is 880×192 with a full contribution grid and both light/dark files present on `gh-pages`. Metrics run `35430368890` succeeded and repository count matched GitHub GraphQL. The linked Pages route returned HTTP 200 and rendered correctly in a live Chrome screenshot. README link was pushed in commit `16f3202` after rebasing over metrics workflow updates.
- Known issues: The account-wide token is an OAuth credential stored as a GitHub Actions secret and can be revoked or expire; if that happens, the metrics workflow will need a replacement secret. GitHub may cache the profile page briefly after a push.
- Failed or abandoned approaches: The first snake workflow used the wrong `outputs` input and lacked `contents: write`; both were corrected and the workflow rerun successfully.
- Verification: The token-backed generator passed locally and GitHub Actions run `35442429967` succeeded after adding checkout. The public `gh-pages` snake is now regenerated from the rolling 365-day GraphQL contribution calendar rather than the generic snake action. The current token returned 98 contributions; the user's screenshot showed 182, so the remaining difference is GitHub's native/private-contribution display scope and must not be presented as an exact 182 match.
- Next steps: If the public image still differs from the logged-in native total, the account token needs the same contribution visibility as the native page; monitor token expiry.
